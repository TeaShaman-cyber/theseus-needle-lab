# Needle framing 2×2

Issue: #12

Zero-training probe of the hypothesis that the earlier routing task conflicts with Needle's base tool-applicability/refusal contract.

The four arms reuse the same 12 evidence statements and labels. Only two factors vary: a semantic description on the `route` schema and an explicit instruction prefix asking the model to classify with `route`.

Direct grammar canaries run first with the original bare schema. Results are descriptive only; the workflow does not assert that canaries or any arm must succeed.
