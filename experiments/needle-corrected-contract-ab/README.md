# Corrected tool-contract A/B

Issue #14 isolates whether Needle's old tool-applicability contract blocked microlearning.
The exact 3-epoch old-contract W4 artifact from run 33432515778 is a frozen control.
Only one new arm is trained: same labels and hyperparameters, but a described `route` schema plus explicit classification framing.
All models are evaluated under the same corrected framing. Primary endpoints are train-replay valid call rate and decision accuracy.
